	    <!-- Start Nav Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top lead">
        <div class="container  ">
            <a class="navbar-brand" href="#">MY COMPANY</a>
            <!-- Ce boutton apparaitera en responsive -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item ms-2">
                        <a class="nav-link" aria-current="page" href="{{path('app_home')}}">Accueil</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="nav-link" aria-current="page" href="#services">comment ca marche!</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="nav-link" aria-current="page" href="#about">à propos de nous</a>
                    </li>
                  
                    <li class="nav-item ms-2">
                        <a class="nav-link" aria-current="page" href="#contact">Contact</a>
                    </li>
					

					{% if app.user %}
					<li class="nav-item ms-2">
                        <a class="nav-link" aria-current="page" href="{{#}}">profil</a>
                    </li>
					<li class="nav-item ms-2">
                        <a class="nav-link" aria-current="page" href="{{path('app_logout')}}">Me deconnecter</a>
                    </li>
					{% else %}
							<li class="nav-item ms-2">
                        <a class="nav-link" aria-current="page" href="{{path('app_register')}}">M'inscrire</a>
                    </li>
					<li class="nav-item ms-2">
                        <a class="nav-link" aria-current="page" href="{{path('app_login')}}">Me Connecter</a>
                    </li>
					{% endif %}
                </ul>
            </div>
        </div>
    </nav>
    <!-- End Nav section -->
